# 01: immutability of smart contracts
## external dependencies
- if your smart contract has external dependencies, it's recommended to have a method to udpate those.
- Access to such methods should be controlled
